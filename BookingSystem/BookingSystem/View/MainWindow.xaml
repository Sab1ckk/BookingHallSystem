<Window x:Class="BookingSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingSystem"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280">
    <Window.Resources>
        <Style x:Key="SideBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/> 
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="10,0,20,10"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" 
                                    Property="Background" 
                                    Value="Transparent"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" 
                                    Property="Background" 
                                    Value="#FF004080"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" 
                                    Property="Background" 
                                    Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FFFD7F67"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="UsHover" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FFFD7F67"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Expander Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" ExpandDirection="Right" Background="#FF1F1A5B">
            <StackPanel>
                <Button Content="Главная" Margin="0,30,10,0" Command="{Binding OpenHomeCommand}" Style="{StaticResource SideBarButton}"/>
                <Button Content="Бронирования" Margin="0,10,10,0" Command="{Binding OpenReservationCommand}" Style="{StaticResource SideBarButton}"/>
                <Button Content="Пользователи" Margin="0,10,10,0" Command="{Binding OpenUsersCommand}" Style="{StaticResource SideBarButton}"/>
                <Button Content="Мероприятия" Margin="0,10,10,0" Command="{Binding OpenEventsCommand}" Style="{StaticResource SideBarButton}"/>
                <Button Content="Отчеты" Margin="0,10,10,0" Command="{Binding OpenReportCommand}" Style="{StaticResource SideBarButton}"/>
                <Separator Height="30" Background="Transparent"/>
                <Button Content="База данных" Margin="0,10,10,0" Command="{Binding OpenHomeCommand}" Style="{StaticResource SideBarButton}"/>
            </StackPanel>
        </Expander>

        <Menu Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right">
            <MenuItem Header="Выйти" Margin="5" FontFamily="Bahnschrift" FontSize="20"/>
        </Menu>

        <Frame Content="{Binding CurrentPage}" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
