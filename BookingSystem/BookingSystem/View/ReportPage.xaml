<Page x:Class="BookingSystem.View.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:BookingSystem.View"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1100"
      Title="Отчеты системы">

    <Page.Resources>
        <!-- Стиль для кнопок отчетов -->
        <Style x:Key="ReportButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Padding" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10" 
                                BorderBrush="#E2E8F0" 
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8FAFC"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFFD7F67"/>
                                <Setter Property="Foreground" Value="#FFFD7F67"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EDF2F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для DataGrid с авто-колонками -->
        <Style x:Key="ModernReportGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="45"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Style.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F1F5F9"/>
                    <Setter Property="Foreground" Value="#64748B"/>
                    <Setter Property="Padding" Value="15,12"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F8FAFC"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="15,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#1E293B"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Tabs/Buttons -->
            <RowDefinition Height="*"/>
            <!-- Data Card -->
        </Grid.RowDefinitions>

        <!-- 1. Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Аналитика и Отчеты" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Выберите тип отчета для формирования актуальных данных" Foreground="#64748B" FontSize="14"/>
        </StackPanel>

        <!-- 2. Панель инструментов (Кнопки) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
            <Button Content="📊 Свободные залы" 
                    Command="{Binding FreeRoomsCommand}" 
                    Style="{StaticResource ReportButtonStyle}" VerticalContentAlignment="Center"/>

            <Button Content="📅 Бронирования (месяц)" 
                    Command="{Binding MonthReservationsCommand}" 
                    Style="{StaticResource ReportButtonStyle}"/>

            <Button Content="🏆 Работник месяца" 
                    Command="{Binding BestEmployeeCommand}" 
                    Style="{StaticResource ReportButtonStyle}"/>
        </StackPanel>

        <!-- 3. Карточка отчета -->
        <Border Grid.Row="2" Background="White" CornerRadius="15" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="1" Opacity="0.05"/>
            </Border.Effect>

            <DockPanel>
                <!-- Динамический заголовок отчета -->
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                    <TextBlock Text="{Binding ReportTitle, FallbackValue='Выберите отчет'}" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Foreground="#1E293B"/>
                    <Rectangle Height="2" Fill="#F1F5F9" Margin="0,10,0,0" HorizontalAlignment="Stretch"/>
                </StackPanel>

                <!-- Таблица данных -->
                <DataGrid ItemsSource="{Binding ReportData}" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True" 
                          Style="{StaticResource ModernReportGrid}"/>
            </DockPanel>
        </Border>
    </Grid>
</Page>